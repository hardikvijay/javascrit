const myObject={
    js:"javascript",
    cpp:"C++",
    rb:"ruby",
    py:"python"
}
for (const key in myObject) {
    /*console.log(key)
    console.log(myObject[key])
    console.log(`${key} shortcut is ${myObject[key]}`)*/

                                                                     //for object we use (forin loop)
}

const program=["java","cpp","rb","py"]
for (const keys in program) {
  console.log(keys)
  console.log(program[keys])                        //forin loop nhi lgega map me
}
